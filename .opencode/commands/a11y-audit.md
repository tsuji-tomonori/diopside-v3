---
description: 変更差分をWCAG 2.2 AA観点で監査する
---

次の順で実施する。
1. `@a11y-auditor` を呼び出して、対象差分を監査する。
2. 必要なスキルをロードする（例: `a11y-keyboard-focus`, `a11y-forms-errors`）。
3. 指摘は `Blocker/High/Medium/Low` と関連達成基準を必ず付ける。

対象: $ARGUMENTS
