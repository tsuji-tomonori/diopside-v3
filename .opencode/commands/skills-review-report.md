---
description: スキル見直しの進捗と残件を確認する
---

次の順で実施する。
1. `python3 .opencode/skills/skill-maintainer/scripts/skills_review.py report $ARGUMENTS` を実行する。
2. `pending` と `next_candidates` を確認し、次に見る1件を決める。
3. 必要なら `/skills-review-next` で優先対象を1件だけ取得する。

対象: $ARGUMENTS
