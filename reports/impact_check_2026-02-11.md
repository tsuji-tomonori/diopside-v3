# 影響確認レポート

- 日付: 2026-02-11
- 対象: `RQ-FR-001`〜`RQ-FR-018`, `RQ-RDR-017`
- 実施内容: 機能要求の定型テンプレート記述を廃止し、FR単位の受入基準・例外・テスト観点へ具体化。

## 影響確認
- 要求→要求:
  - `RQ-FR-001`〜`RQ-FR-018` の `要求` 本文を機能固有に更新し、FR単位の判定可能性を向上。
  - `up`（`RQ-UC-*`）をユースケース意図に合わせて再確認。
- 要求→決定記録:
  - `RQ-RDR-017` を追加し、FR具体化の決定根拠と影響を記録。
- 要求→設計:
  - `related` の `BD-API-001` / `BD-UI-001` 参照を維持し、設計トレース導線を保持。

## 判定
- FR群を「タイトル差分のみ」状態から脱却し、個別レビュー可能な粒度へ更新済み。
- 文書リンク整合は `obsidian-doc-check` 実行で確認する。

## 追記（FR個別確認チェックリスト追加）
- 対象: `RQ-FR-001`〜`RQ-FR-018`
- 実施: 各FRに `## 個別確認チェックリスト` を追加し、確認主体・重点確認対象・確認方法を機能別に定義。
- 反映: FR全件の `version` を `1.0.3` へ更新。
- 判定: 同一フォーマットでも確認観点がFR単位で分離され、レビュー時の見落としを抑制できる構成になった。
